[project]
name = "async-api-cli"
version = "0.1.0"
description = "Project for Python Training"
authors = [{name="Your Name"}]
dependencies = [
    "httpx", "requests"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
